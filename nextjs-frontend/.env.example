# URL of backend
NEXT_PUBLIC_API_URL=http://localhost:4000
# Long randomized string to encrypt the backend session token
# generate one in terminal: node -p "crypto.randomBytes(64).toString('hex')"
AUTH_SECRET=